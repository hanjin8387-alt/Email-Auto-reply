# UI/UX Review Report — MailTriageAssistant
> Date: 2026-02-13
> Reviewer: Agent 02 (UI/UX)

## Summary
- 총 이슈: 12
- Critical: 2 | Major: 5 | Minor: 4 | Info: 1

## Current State Assessment
- 단일 윈도우 2컬럼 레이아웃 (좌: 이메일 목록, 우: 상세)
- `StaticResource` 기반 색상 시스템 적용 완료 (App.xaml)
- `AutomationProperties`, `TabIndex`, `ToolTip` 대부분 적용
- Empty State (빈 리스트/미선택) 구현 완료
- ListBox 가상화 및 커스텀 선택/호버 스타일 적용
- `RedactionConverter`로 Sender/Subject 마스킹
- 카테고리 필터 ComboBox + 아이콘 뱃지 구현

---

## Findings

### 🔴 Critical

| # | 카테고리 | 파일:줄 | 이슈 | 권장사항 |
|---|---|---|---|---|
| C-01 | 확장성 | `MainWindow.xaml:전체` | 단일 윈도우에 모든 기능이 집중됨. VIP 관리, 설정, 통계 등 신규 기능 추가 시 **레이아웃 과부하** | NavigationView 또는 TabControl 기반 멀티페이지 구조 도입. 또는 별도 SettingsWindow 분리 |
| C-02 | 다국어 | `MainWindow.xaml:34-82` | 모든 UI 문자열이 XAML에 직접 하드코딩됨 (`"메일 분류 실행"`, `"우선순위 메일"` 등). 다국어 UI 불가 | `ResourceDictionary` 기반 문자열 리소스 외부화 (`Strings.ko.xaml`, `Strings.en.xaml`) |

### 🟡 Major

| # | 카테고리 | 파일:줄 | 이슈 | 권장사항 |
|---|---|---|---|---|
| M-01 | 레이아웃 | `MainWindow.xaml:269-325` | 우측 하단 버튼 5개가 수평 나열 — **1100px 미만에서 버튼 텍스트 잘림** | `WrapPanel` 또는 `UniformGrid` 적용, 또는 ToolBar 패턴 |
| M-02 | 피드백 | `MainWindow.xaml:61-67` | ProgressBar가 상단 DockPanel에만 존재. **본문 로딩 시 별도 피드백 없음** (이미 IsLoading 바인딩은 있으나 위치가 멀어 눈에 안 띔) | 본문 영역 내 오버레이 로딩 인디케이터 추가 |
| M-03 | 네비게이션 | 없음 | 설정 화면/VIP 관리 등 **앱 내 네비게이션 구조 부재** | 좌측 사이드바 또는 설정 버튼 → 모달/별도 창 |
| M-04 | 반응형 | `MainWindow.xaml:29-31` | `2*:3*` 고정 비율 — 작은 화면에서 좌측 패널 폭 부족 | `MinWidth` 설정 + `GridSplitter` 검토 |
| M-05 | 에러 상태 | `MainViewModel.cs:StatusMessage` | 에러 발생 시 StatusBar 텍스트만 변경되어 **사용자가 놓칠 수 있음** | 에러 시 StatusBar 배경색 빨강 변경 + 3초 애니메이션 |

### 🟢 Minor

| # | 카테고리 | 파일:줄 | 이슈 | 권장사항 |
|---|---|---|---|---|
| m-01 | 타이포그래피 | `MainWindow.xaml:전체` | FontFamily 미지정 → 시스템 기본 폰트 사용 | `App.xaml`에 `FontFamily="Malgun Gothic, Segoe UI"` 지정 |
| m-02 | 아이콘 | `MainWindow.xaml:174-178` | 📎 이모지 TextBlock — Windows 버전별 렌더링 차이 | Path 기반 아이콘 또는 Font Awesome 검토 |
| m-03 | 앱 아이콘 | 없음 | 앱 아이콘(`.ico`) 미설정 — 기본 WPF 아이콘 표시 | 전용 아이콘 생성 + `.csproj`에 `<ApplicationIcon>` |
| m-04 | 키보드 | `MainWindow.xaml:전체` | 키보드 단축키(Shortcut) 미정의 — `Ctrl+R`(분류), `Ctrl+D`(Digest) 등 | `InputBindings` + `KeyBinding` 추가 |

### ⚪ Info

| # | 카테고리 | 이슈 |
|---|---|---|
| I-01 | 긍정 | 접근성 속성(AutomationProperties, TabIndex, ToolTip) 8개 인터랙티브 요소에 적절히 적용됨 ✅ |

---

## Feature Impact (신규 기능 반영 시)

| 변경점 | 영향 UI 요소 | 필요 작업 |
|---|---|---|
| 시스템 트레이 | `App.xaml.cs`, `MainWindow.xaml.cs` | NotifyIcon 리소스, Closing 이벤트 핸들러 |
| VIP 관리 UI | 신규 `SettingsWindow.xaml` | 별도 창 또는 설정 탭 |
| 자동 분류 | `MainWindow.xaml` 상단 | 타이머 상태 표시 (`다음 분류: N분 후`) |
| 세션 통계 | `MainWindow.xaml` 하단 또는 사이드 | 카테고리 분포 바 차트 또는 숫자 카드 |
| 다국어 | 모든 텍스트 요소 | ResourceDictionary 전환 메커니즘 |

---

## Codex Handoff — Task List

| # | 파일 | 변경 요지 | 테스트 커맨드 | 수용 기준 | 위험도 |
|---|---|---|---|---|---|
| T-01 | `App.xaml` | 전역 `FontFamily` 리소스 추가 | `dotnet build` | 빌드 성공 + 일관된 폰트 | Low |
| T-02 | `MainWindow.xaml:269-325` | 버튼 영역 WrapPanel 전환 | `dotnet build` | 900px에서 줄바꿈 정상 | Low |
| T-03 | `MainWindow.xaml` | 본문 영역 로딩 오버레이 추가 | `dotnet build` | 빌드 성공 + 로딩 표시 | Low |
| T-04 | `Resources/Strings.ko.xaml` (신규), `Resources/Strings.en.xaml` (신규) | 다국어 리소스 딕셔너리 생성 | `dotnet build` | 빌드 성공 | Medium |
| T-05 | `MainWindow.xaml`, `App.xaml` | 모든 하드코딩 문자열 → `DynamicResource` | `dotnet build` | 빌드 성공 + 한국어 유지 | High |
| T-06 | `MainWindow.xaml`, 신규 `SettingsWindow.xaml` | 설정 버튼 + 설정 창 (VIP 관리 포함) | `dotnet build` | 빌드 성공 | Medium |
| T-07 | `MainWindow.xaml` 상단 | 자동 분류 상태 표시 (`"다음 분류: Nm 후"`) | `dotnet build` | 빌드 성공 + 타이머 표시 | Low |
| T-08 | `MainWindow.xaml`, `App.xaml` | 에러 시 StatusBar 배경색 변경 (Red → 3초 후 복원) | `dotnet build` | 에러 시 시각 피드백 | Low |
| T-09 | `MainWindow.xaml` | 키보드 단축키 `InputBindings` 추가 | `dotnet build` | Ctrl+R/D/O 동작 | Low |
| T-10 | `.csproj`, 아이콘 파일 | 앱 아이콘 설정 | `dotnet build` | 타이틀바 + 작업표시줄 아이콘 | Low |
